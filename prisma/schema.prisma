
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("MONGODB_URI")
}

model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}